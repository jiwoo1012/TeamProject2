// Page placeholder.
import PagePlaceholder from '../../components/common/PagePlaceholder'
import styles from './OrderComplete.module.scss'

const OrderComplete = () => <PagePlaceholder title="주문 완료" styles={styles} />

export default OrderComplete
